const config = {
    type: 'mysql',
    host: 'localhost',
    port: 53,
    username: 'root',
    password: 'root',
    database: 'test',
    entities: [],
    synchronize: true,
}

export default config;